//array is collection of similar data types stored at contiguous(continuous) memory locations locations.

#include <stdio.h>

int main(){
    int marks1 = 10;
    int marks2 = 20;
    int marks3 = 30;

    int marks[] = {10,20,30};
    printf("%d", marks[0]);
    return 0;
}